name: test
channels:
  - conda-forge
  - omnia
dependencies:
    # Base depends
  - python
  - pip
  - fastapi
  - cmiles

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

